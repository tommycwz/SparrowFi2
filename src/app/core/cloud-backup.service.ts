// This file is obsolete and unused. SparrowFi no longer has a local file
// to optionally back up - your account's data now lives directly in
// Supabase and loads/saves automatically (see `cloud-data.service.ts` and
// `StateService.load()`/`save()`). Safe to delete this file entirely.
