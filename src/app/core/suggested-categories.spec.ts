/**
 * RETIRED - see suggested-categories.ts. This suite covered the removed
 * "Load Suggested Categories" feature and no longer has anything to test.
 * Kept as an inert placeholder (so CI doesn't fail on stale expectations)
 * only until both files can be deleted outright - please `git rm` this file
 * and suggested-categories.ts next time you're working in the repo
 * directly, then commit/push.
 */
describe('suggested-categories (retired)', () => {
  it('is an inert placeholder - delete this file and suggested-categories.ts', () => {
    expect(true).toBe(true);
  });
});
