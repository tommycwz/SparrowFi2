// This file is obsolete and unused. Cloud Backup used to map a made-up
// username to a synthetic email domain so people wouldn't need a "real"
// email; Supabase's auth backend actively rejects fake domains now (see
// the comment in cloud-auth.service.ts for why), so sign-in uses a real
// email address instead. Safe to delete this file entirely.
